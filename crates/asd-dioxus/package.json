{
  "name": "asd-dioxus",
  "private": true,
  "description": "Webview JS dependencies for the asd Dioxus client, bundled into one minified file",
  "scripts": {
    "build": "esbuild assets/vendor-entry.js --bundle --minify --format=iife --outfile=dist/vendor.js"
  },
  "dependencies": {
    "ghostty-web": "^0.4.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
